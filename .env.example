# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys

# Your OpenAI API key (required)
OPENAI_API_KEY=your-api-key-here

# Model to use for code generation (optional, defaults to gpt-4)
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
MODEL_NAME=gpt-4

# Maximum tokens to generate (optional, defaults to 1000)
MAX_TOKENS=1000

# Temperature for randomness (optional, defaults to 0.7)
# Range: 0.0 (deterministic) to 2.0 (very random)
TEMPERATURE=0.7
